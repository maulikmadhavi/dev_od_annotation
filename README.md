# Introdution
This is simple lite weight web application to perform the object detection annotation. Input is the images and labels are yolo formatted text files.  These are baseline annotation to be used to be shown on the UI page. Next, the verifier can make adjustment with "A" (add), "D" (delete) and "M" (modify) buttons. Finally, the annotation can be exported in yolo format text files.




 